Populated by `/teach`. Each lesson is a self-contained HTML file, `NNNN-<dash-case-name>.html`,
opened in the browser and worked through before the matching chapter is written. See `WORKFLOW.md`
for the per-chapter loop.

**Do not read a lesson in order to copy its structure.** Each is 30-50 KB and they are all built
from the same components; `../../assets/LESSON-TEMPLATE.html` states that contract in a fraction of the
size. Open a lesson only when the question is about that lesson's own content.
