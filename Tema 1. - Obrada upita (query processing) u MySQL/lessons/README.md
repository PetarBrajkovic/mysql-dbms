Populated by `/mattpocock-skills:teach`. Each lesson is a self-contained HTML file,
`0001-<dash-case-name>.html`, opened in the browser and worked through before the matching chapter
is written. See `WORKFLOW.md` for the per-chapter loop.
