Populated by `/teach`. Compressed reference material distilled from the lessons - cheat sheets,
quick syntax references - as `*.html` files designed to print well and be scanned fast.

These are for the user, not for orientation: a reference card restates its lesson, so reading one to
find out what a chapter covered costs more than `learning-records/README.md`, which is the index
built for that.
