# MySQL dumps and generated data
*.sql.gz
*.dump
examples/**/*.csv
data/

# pandoc / export intermediates
*.docx.tmp
*.pdf.tmp
.pandoc/

# OS / editor
Thumbs.db
desktop.ini
.DS_Store
.vscode/

# Claude Code agent worktrees and local state
.claude/

# Course lecture PDFs - source material, not project output.
# The exported paper (rad.docx / rad.pdf) is NOT covered by this and stays committable.
Predavanja/
